<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{{ title }}</title>
</head>
<body>
	<h1>{{ title }}</h1>
	<table class="table">
		<thead>
			{% for column in columns %}
				<th>{{ column }}</th>
			{% endfor %}
		</thead>
		<tbody>
			
			{% include 'components/item.html.twig' with {'itens': pessoas} %}
			
		</tbody>
	</table>
</body>
</html>